## Domains

Domains are endpoints that accept HTTP, HTTPS and TLS traffic. Claim one free static domain per user or upgrade to paid to use a domain of your choice.
![ngroker domains](./../../../public/screenshots/domains.png)
